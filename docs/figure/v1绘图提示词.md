Create a clean, publication-quality scientific figure for a computer vision / brain-computer interface paper. The figure is a model overview diagram for a framework named “CineBrain-SF v1”. Use a modern vector infographic style, white background, neat layout, soft shadows, minimal but clear text, suitable for a NeurIPS / CVPR / ICCV paper. Make the diagram wide, horizontal, and easy to read.

Main idea:
The model reconstructs continuous video from synchronized multimodal brain signals under naturalistic audiovisual stimulation. It uses slow-fast disentanglement:
1) a Slow Semantic-Structure Branch dominated by fMRI,
2) a Fast Motion-Dynamics Branch dominated by EEG,
3) a Cross-Modal Gated Fusion module,
4) a Multi-Guidance Neuro-Latent Decoder based on a video diffusion model.

Layout from left to right:

Far left: “Naturalistic Audiovisual Stimulus”
- show a small video frame strip plus an audio waveform / speaker icon
- below it, show synchronized brain recordings:
  - fMRI signals
  - EEG signals
- make the setup clearly multimodal and synchronized

Input block:
- fMRI input: visual ROI + auditory ROI, lag-corrected
- EEG input: windowed 64-channel EEG sequence
Use blue tones for fMRI and orange tones for EEG.

Middle upper branch: “Slow Semantic-Structure Branch (fMRI-dominant)”
Inside this block, show:
- fMRI encoder
- audiovisual context adapter
- keyframe head
- scene-text head
- structure head
Outputs from this branch:
- predicted keyframe
- scene-text embedding
- structure latent
Visually emphasize that this branch captures slow-varying semantic and structural information.

Middle lower branch: “Fast Motion-Dynamics Branch (EEG-dominant)”
Inside this block, show:
- EEG spatial encoder
- EEG temporal encoder
- dynamics head
- motion head
- temporal coherence head
Outputs from this branch:
- dynamic pattern
- motion latent / flow token
- temporal coherence token
Visually emphasize that this branch captures fast-varying motion and temporal dynamics.

Center fusion block:
“Cross-Modal Gated Fusion”
- show the slow branch and fast branch feeding into a central fusion module
- fusion outputs:
  - fused brain latent z_b
  - adaptive weights alpha_key, alpha_txt, alpha_mot, alpha_brain
  Make this block purple or violet and show that it adaptively weights different guidance signals.

Right block:
“Multi-Guidance Neuro-Latent Decoder”
- depict a CogVideoX-style or generic video diffusion decoder
- show four incoming guidance arrows:
  1. keyframe guidance
  2. scene-text guidance
  3. motion guidance
  4. fused brain latent guidance
- decoder outputs:
  - reconstructed video clip
  Also show optional side outputs:
- predicted keyframe
- motion map / flow token visualization
- semantic embedding

At the bottom of the figure, add a slim “Training Supervision” strip showing the offline cached supervision targets:
- keyframe image embedding
- scene/fusion text embedding
- structure latent
- motion / optical flow target
- dynamics label
These supervision targets should point upward to the relevant heads in both branches.

Visual design requirements:
- Use elegant arrows and grouped modules
- Use blue for fMRI-related components, orange for EEG-related components, purple for fusion, green for decoder / output
- Keep text concise and readable
- Use labels like “Slow branch”, “Fast branch”, “Fusion”, “Decoder”, “Offline supervision targets”
- Show that the model differs from a single unified latent baseline by explicitly separating semantic-structure and motion-dynamics pathways
- The final style should look like a polished paper figure, not a cartoon

Important scientific emphasis:
- synchronized fMRI + EEG inputs
- naturalistic audiovisual setting
- slow-fast role assignment
- explicit semantic / structure vs motion / dynamics decomposition
- adaptive multi-guidance video generation